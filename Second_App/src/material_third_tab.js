// height이랑 width로 된거 flex로 나중에 바꿔줘야함!!!!!

import * as React from 'react';
import {theme} from '../colors';
import {useState, useEffect} from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
  TouchableHighlight,
  Image,
  TouchableWithoutFeedback,
  TextInput,
  TouchableOpacity,
} from 'react-native';

const MaterialThirdScreen: () => Node = () => {
  // class TabHomeScreen extends Component {
  return (
    <View>
      <Text>Material Third Tag</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default MaterialThirdScreen;
